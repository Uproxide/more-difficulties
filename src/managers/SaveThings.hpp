#pragma once

#include <Geode/Geode.hpp>

using namespace geode::prelude;

class SaveThings {
    public:
        inline static bool casual = true;
        inline static bool tough = true;
        inline static bool cruel = true;
};