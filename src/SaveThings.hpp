#pragma once

#include <Geode/Geode.hpp>

using namespace geode::prelude;

class SaveThings {
    public:
        inline static bool casual;
        inline static bool tough;
        inline static bool cruel;
};